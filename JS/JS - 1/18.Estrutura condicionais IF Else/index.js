const idade = prompt("informe sua idade")

if ( idade > 18) {
    alert   ("Voce é maior de idade")
} else if ( idade > 12) {
    alert("Voce é menor de idade")
} else if ( idade > 4 ) {
    alert ("você é ciranca")
} else {
    alert (" . . . ")
}

// o simbolo ? é um valor ternario para criar comparação.
//